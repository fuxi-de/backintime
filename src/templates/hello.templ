package templates

templ Hello(name string) {
	<div>Hello, { name }</div>
	<p>
		hallo
	</p>
	<span>world</span>
	<p>again</p>
	<button type="button">Login</button>
}

templ HomePage(name string) {
	@Layout(Hello(name))
}
